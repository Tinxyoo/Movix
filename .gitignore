# ── Secrets & 本地配置(绝不要提交)─────────────────
.env
.env.*
!.env.example

# ── Rust 构建产物 ──────────────────────────────────
/target/
**/*.pdb
**/*.exe

# ── 日志 / 临时文件 ────────────────────────────────
*.log
clippy*.txt

# ── 本地任务/发布草稿(不是源码)────────────────────
jobs.json
release_jobs.json

# ── 编辑器 / 系统 ──────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
